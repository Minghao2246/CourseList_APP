package ui.panels.title;

import javax.swing.*;
import java.awt.*;

// create title panel
public class TitleNGC extends JPanel {
    public TitleNGC() {
        setLayout(new FlowLayout());
        setBackground(new Color(248, 213, 231));
        setPreferredSize(new Dimension(1, 10));
    }
}